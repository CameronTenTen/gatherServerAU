name = admin
users = Gurin; rymcd; Juaro; Leo;
roles = rcon;
commands = assignseclev; assignseclevid; ban; banhost; banid; freezeid; help; kick; kickhid; kickhost; kickid; list; listbans; login; msg; mute; muteid; nextmap; players; printseclevs; restartmap; swapid; unban; unbanhost; unfreezeid; unmute; unmuteid; printseclevs;
features = admin_color; can_join_teams; always_change_team; ban_immunity; freeze_immunity; ignore_immunity; join_full; kick_immunity; map_vote; mark_any_team; mark_player; mute_immunity; name_mouseover_spectator; pingkick_immunity; skip_votewait; spectator; view_collapses; view_rcon; 
assign = admin; players; spectator;
